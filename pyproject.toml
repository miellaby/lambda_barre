[project]
name = "lambda-barre"
version = "0.0.1"
description = "Lambda barre — a learning desktop-pet animat. Stage 1: actuated body."
requires-python = ">=3.11"
dependencies = [
    "pymunk>=7.3",
    "pygame-ce>=2.5",
    "torch>=2.0",
]

[project.scripts]
lambda-barre = "lambda_barre.main:run"
