# lambda_barre
2027 desktop-pet project: an animat with simplified morphology, actual machine learning, a world-model transformer, RL on a realistic sensory field, and an innate reward circuit the trainer can leverage to shape the animal's behavior.
